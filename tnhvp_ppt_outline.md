# 非正交稀疏轨道 VBSCF 的非冗余 TNHVP 算法讲稿

> 目标：这份 Markdown 用于准备 5--6 页 PPT，只讲算法思想和数学结构，不包含实验结果，也不引入工程实现细节。

---

## Slide 1. 问题：非正交稀疏轨道优化的困难

### 页面主旨

VBSCF 轨道优化的自由度来自稀疏轨道系数。由于轨道是非正交的，并且每个轨道只在给定局部支撑上展开，直接在所有系数方向上做优化会引入大量冗余或近线性相关方向，使优化问题变得病态。

### 建议放在页面上的内容

- 每个轨道只允许在预设稀疏支撑内变化。
- 非正交度量下，不同轨道方向之间存在强耦合和近冗余。
- 直接处理全部系数方向会导致：
  - 梯度中混入弱物理意义方向；
  - Hessian 条件数变差；
  - 一阶方法进入长尾收敛；
  - 显式二阶方法存储和构造代价过高。

若独立优化维度为 \(N\)，显式 Hessian 为

\[
H \in \mathbb{R}^{N\times N}.
\]

其存储为

\[
O(N^2),
\]

密集对角化通常为

\[
O(N^3).
\]

### 讲述重点

这一页要回答“为什么需要新算法”。核心不是普通优化器不够好，而是原始系数空间本身包含大量不适合直接优化的方向。因此应先构造一个物理上更有效、数值上更稳定的非冗余局部空间。

---

## Slide 2. 非冗余局部空间：构造 \(U_p\)

### 页面主旨

对每个轨道 \(p\)，在它自己的稀疏支撑内构造一组局部候选方向，并通过筛选和正交化得到非冗余局部基 \(U_p\)。后续梯度、步长和二阶作用都在这个空间中表达。

### 数学表达

设第 \(p\) 个轨道在其稀疏支撑上的系数为

\[
x_p \in \mathbb{R}^{n_p}.
\]

首先在当前局部支撑内生成候选方向集合

\[
B_p = [b_1,b_2,\ldots,b_r] \in \mathbb{R}^{n_p\times r}.
\]

这些候选方向来自当前轨道所在局部空间中与其互补的占据方向和未占据方向，并且只在该轨道允许的稀疏支撑上取值。

为了避免近零方向和线性相关方向破坏数值稳定性，先丢弃范数过小的列：

\[
\|b_j\| \leq \tau \|x_p\| \quad \Rightarrow \quad b_j \text{ removed},
\]

其中 \(\tau\) 是很小的阈值。

随后对剩余候选方向做两遍修正 Gram-Schmidt 正交化，得到

\[
U_p^T U_p = I.
\]

于是任意允许的局部扰动可写为

\[
\delta x_p = U_p z_p.
\]

### 物理含义

- \(U_p\) 保留当前稀疏支撑内真正可用的变化方向。
- 近零或近相关方向被移除，减少病态自由度。
- 优化坐标从原始系数空间转为非冗余坐标 \(z_p\)。
- 每个轨道局部处理，符合稀疏轨道的局域结构。

### 讲述重点

这一页是算法核心之一。可以把 \(U_p\) 解释成“当前轨道在给定稀疏支撑内的物理有效局部坐标基”。它不是后处理，也不是单纯数值技巧，而是后续一阶和二阶优化共同使用的坐标系统。

---

## Slide 3. 梯度投影与步长展开

### 页面主旨

有了 \(U_p\) 后，优化不再直接沿原始系数方向前进，而是在非冗余坐标中计算梯度、求步长，再展开回稀疏轨道系数。

### 梯度投影

设能量对第 \(p\) 个轨道局部系数的梯度为

\[
g_p = \frac{\partial E}{\partial x_p}.
\]

在非冗余局部基下，reduced gradient 为

\[
g_{p}^{\mathrm{red}} = U_p^T g_p.
\]

所有轨道的 reduced gradient 拼接为

\[
g^{\mathrm{red}} = \{g_p^{\mathrm{red}}\}_p.
\]

### 步长展开

优化器求得 reduced step

\[
s = \{z_p\}_p,
\]

再对每个轨道展开为

\[
\delta x_p = U_p z_p.
\]

由此得到新的稀疏轨道试探点，并保持更新只发生在允许的稀疏支撑内。

### 页面图示建议

```text
原始梯度
  -> 按轨道取局部片段
  -> 投影到 U_p 坐标
  -> 在 reduced space 中求步长
  -> 展开回稀疏轨道系数
  -> 计算新的 VBSCF 能量和梯度
```

### 讲述重点

这页要强调：\(U_p\) 不只用于二阶算法。它定义了一个统一的非冗余优化坐标，梯度、步长、预条件和 HVP 都在同一坐标中表达。因此算法的一致性来自“同一个局部图表贯穿整个优化过程”。

---

## Slide 4. HVP：不显式构造 Hessian 的二阶信息

### 页面主旨

TNHVP 使用 Hessian-vector product, HVP, 来获得二阶曲率信息，而不是显式构造 Hessian 矩阵。在非冗余坐标中，需要的是 reduced Hessian 对向量的作用。

### 数学表达

设完整 Hessian 为

\[
H = \nabla^2 E.
\]

若 \(U\) 表示所有局部基 \(U_p\) 的整体拼接，则 reduced Hessian 形式上为

\[
H^{\mathrm{red}} = U^T H U.
\]

但实际算法不显式形成 \(H^{\mathrm{red}}\)，只需要计算

\[
v \mapsto H^{\mathrm{red}}v.
\]

即

\[
H^{\mathrm{red}}v = U^T H (Uv).
\]

### 为什么有优势

显式 Hessian 方法需要

\[
O(N^2)
\]

存储，并可能需要

\[
O(N^3)
\]

级别的密集线性代数。

HVP 方法只保留当前向量、若干 Krylov 向量和少量历史信息，存储近似为

\[
O(mN), \quad m \ll N.
\]

内层迭代的主要成本可写为

\[
O(m C_{\mathrm{HVP}} + mN),
\]

其中 \(C_{\mathrm{HVP}}\) 是一次 HVP 作用的实际电子结构计算成本。

### 讲述重点

不要把 HVP 简化成“一次计算就是 \(O(N)\)”。准确说法是：HVP 避免了显式 Hessian 的平方存储和三次对角化，同时通过少量矩阵-向量作用保留二阶曲率信息。

---

## Slide 5. 截断 Newton 与信赖域

### 页面主旨

在 reduced space 中，TNHVP 近似求解二阶模型，但只允许步长落在当前局部图表可信范围内。信赖域用于控制步长，防止非线性外推过大。

### 二阶模型

在当前点附近，能量的 reduced 二阶模型写作

\[
m(s) = E_0 + (g^{\mathrm{red}})^T s + \frac{1}{2}s^T H^{\mathrm{red}}s.
\]

目标是在信赖域内近似求解

\[
\min_s m(s), \qquad \|Us\| \leq \Delta.
\]

这里 \(\Delta\) 是信赖域半径，\(\|Us\|\) 表示 reduced step 展开后在实际稀疏系数空间中的尺度。

### 内层 Krylov 求解

- 以 \(-g^{\mathrm{red}}\) 为右端项建立 Newton 方程的近似求解。
- 每一步只需要 HVP：\(p \mapsto H^{\mathrm{red}}p\)。
- 遇到负曲率时截断，避免沿不可靠方向过度前进。
- 如果步长触及信赖域边界，则在边界上选择模型下降最大的方向。
- 使用简单局部曲率信息和历史 secant 信息作为预条件，改善 Krylov 收敛。

### 步长接受

计算真实能量下降和模型预测下降的比值

\[
\rho = \frac{E(x)-E(x+s)}{m(0)-m(s)}.
\]

- \(\rho\) 足够大：接受该步，并可适当扩大信赖域；
- \(\rho\) 太小：拒绝该步，缩小信赖域后重新求解；
- 若非冗余空间维数发生变化，则重建局部坐标。

### 讲述重点

这一页要说明 TNHVP 不是盲目使用 Newton 步，而是在“非冗余坐标 + HVP 二阶模型 + 信赖域接受准则”三者共同作用下生成稳定步长。

---

## Slide 6. 算法流程总结

### 页面主旨

把完整算法压缩成一张流程图，作为进入实验结果之前的总览。

### 流程图

```text
当前非正交稀疏轨道
        |
        v
按局部支撑构造候选方向 B_p
        |
        v
去除近零/近相关方向，正交化得到 U_p
        |
        v
梯度投影到 reduced space
        |
        v
构造 reduced HVP 作用
        |
        v
Krylov / 截断 Newton 求解信赖域子问题
        |
        v
展开 reduced step，生成新的稀疏轨道试探点
        |
        v
根据真实能量下降和 trust ratio 接受或拒绝
        |
        v
更新局部坐标并进入下一轮迭代
```

### 一句话总结

该方法的核心是：先用 \(U_p\) 把非正交稀疏轨道优化限制到物理有效的非冗余局部空间，再在该空间中用 HVP 型截断 Newton 方法获取二阶曲率信息，从而避免显式 Hessian 的存储、构造和对角化瓶颈。

---

## 备用讲解：30 秒版本

这套算法首先在每个轨道的稀疏支撑内构造局部候选方向 \(B_p\)，去除近零和近相关方向后得到正交基 \(U_p\)。梯度被投影到 \(U_p\) 定义的非冗余坐标中，优化步也先在这个坐标里求解，再展开回稀疏轨道系数。二阶信息不通过显式 Hessian 获得，而是通过 HVP 在 Krylov/截断 Newton 过程中反复调用。信赖域和真实能量下降用于判断步长是否可信。这样既保留二阶曲率信息，又避免显式 Hessian 的高存储和高计算代价。
