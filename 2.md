# Unique Spin-String Pair 的 RI + Low-Rank Update 方案

## 1. 目标

设活性轨道数为 \(N\)，闭壳层活性电子数为 \(M\)，每个自旋通道电子数为

$$
m=\frac{M}{2}.
$$

unique spin string 数量为

$$
U=\binom{N}{m}.
$$

same-spin ordered pair 数量为

$$
N_{\mathrm{pair}}=U^2.
$$

这个 \(U^2\) 很难从精确算法中消掉。因此当前目标是降低每个 pair kernel 的计算标度：

$$
T_{\mathrm{total}}
\sim
U^2 C_{\mathrm{pair}}.
$$

如果 regular pair 的 kernel 能从

$$
C_{\mathrm{pair}}=O(m^3)
$$

降到

$$
C_{\mathrm{pair}}=O(m^2),
$$

总成本就会从

$$
O(U^2m^3)
$$

降到

$$
O(U^2m^2).
$$

RI two-electron 部分的目标类似：

$$
O(U^2N_{\mathrm{aux}}m^3)
\rightarrow
O(U^2N_{\mathrm{aux}}m^2).
$$

这是本方案真正值得做的原因。

## 2. Same-Spin Pair 记号

给定一个 same-spin pair：

$$
L=\{l_1,\ldots,l_m\},
\qquad
R=\{r_1,\ldots,r_m\}.
$$

这里 \(L\) 是 left determinant 的占据轨道，\(R\) 是 right determinant 的占据轨道。

活性轨道 overlap 矩阵记为 \(S^{\mathrm{act}}\)。occupied-overlap 子块为

$$
S_{RL}[a,b]
=
S^{\mathrm{act}}_{r_a,l_b},
\qquad
a,b=1,\ldots,m.
$$

regular pair 满足

$$
d=\det S_{RL}\ne 0,
\qquad
X=S_{RL}^{-1}.
$$

在全活性轨道空间中定义稀疏 transition density：

$$
T_{pq}(R,L)=
\begin{cases}
X_{b,a}, & p=r_a,\ q=l_b,\\
0, & \text{otherwise}.
\end{cases}
$$

注意：\(S_{RL}\) 的行属于 right determinant，列属于 left determinant。因此 \(X_{b,a}\) 对应全空间 transition density 的元素 \(T_{r_a,l_b}\)。

one-electron contribution 可写为

$$
\phi_1(R,L)
=
\sum_{a=1}^m\sum_{b=1}^m
h^{\mathrm{act}}_{r_a,l_b}X_{b,a}
=
\operatorname{Tr}(h^{\mathrm{act}}T).
$$

Hamiltonian 中对应项为

$$
H_1(R,L)=d\,\phi_1(R,L).
$$

## 3. Spin String 单替换遍历

low-rank update 依赖遍历顺序。应使用 Gray-code / revolving-door combination order，使相邻 spin string 只相差一个占据轨道替换。

例如固定 \(L\)，遍历 right string：

$$
R' = R-\{r_i\}+\{r_{\mathrm{new}}\}.
$$

此时 occupied-overlap 子块只改变第 \(i\) 行：

$$
S' = S + e_i u^\mathsf{T},
$$

其中

$$
u_b
=
S^{\mathrm{act}}_{r_{\mathrm{new}},l_b}
-
S^{\mathrm{act}}_{r_i,l_b}.
$$

固定 \(R\)，遍历 left string：

$$
L' = L-\{l_j\}+\{l_{\mathrm{new}}\}.
$$

此时 occupied-overlap 子块只改变第 \(j\) 列：

$$
S' = S + v e_j^\mathsf{T},
$$

其中

$$
v_a
=
S^{\mathrm{act}}_{r_a,l_{\mathrm{new}}}
-
S^{\mathrm{act}}_{r_a,l_j}.
$$

## 4. Sherman-Morrison 更新 \(X\) 和 \(d\)

### 4.1 Row Update

若

$$
S' = S + e_i u^\mathsf{T},
$$

则

$$
\eta = 1 + u^\mathsf{T}X e_i.
$$

determinant 更新为

$$
d' = d\,\eta.
$$

inverse 更新为

$$
X'
=
X
-
\frac{X e_i u^\mathsf{T}X}{\eta}.
$$

其中

$$
X e_i
$$

是 \(X\) 的第 \(i\) 列，

$$
u^\mathsf{T}X
$$

是长度为 \(m\) 的 row vector。

计算量：

$$
O(m^2).
$$

### 4.2 Column Update

若

$$
S' = S + v e_j^\mathsf{T},
$$

则

$$
\eta = 1 + e_j^\mathsf{T}Xv.
$$

determinant 更新为

$$
d' = d\,\eta.
$$

inverse 更新为

$$
X'
=
X
-
\frac{X v e_j^\mathsf{T}X}{\eta}.
$$

其中

$$
Xv
$$

是长度为 \(m\) 的 column vector，

$$
e_j^\mathsf{T}X
$$

是 \(X\) 的第 \(j\) 行。

计算量：

$$
O(m^2).
$$

### 4.3 Singular Fallback

当

$$
|\eta| < \tau_{\mathrm{SM}}
$$

时，当前更新接近奇异，应放弃 low-rank update，回退到现有 `DeterminantOverlapResolver` 的 LU/SVD 路径。

因此第一阶段可稳定实现：

$$
O(m^3)
\rightarrow
O(m^2)
$$

for regular same-spin pairs。

## 5. RI 表示与 \(q_P\)

RI active-space two-electron integral 写为

$$
(pq|rs)
\approx
\sum_{P=1}^{N_{\mathrm{aux}}}
B^P_{pq}B^P_{rs}.
$$

代码中的 `ri_active_pair_factors` 对应

$$
L_{P,\mu(p,q)} \equiv B^P_{pq},
$$

其中 \(\mu(p,q)\) 是 active pair packed index。

对 same-spin pair \((R,L)\)，定义 RI Coulomb projection：

$$
q_P(R,L)
=
\operatorname{Tr}(B^P T)
=
\sum_{a=1}^m
\sum_{b=1}^m
B^P_{r_a,l_b}X_{b,a}.
$$

如果每个 pair 重新计算 \(q_P\)，成本是

$$
O(N_{\mathrm{aux}}m^2).
$$

opposite-spin RI coupling 可以写为

$$
H_{\alpha\beta}
=
d^\alpha d^\beta
\sum_{P=1}^{N_{\mathrm{aux}}}
q^\alpha_P q^\beta_P.
$$

也可以把 determinant 吸收到投影中：

$$
A^\sigma_P = d^\sigma q^\sigma_P,
\qquad
\sigma\in\{\alpha,\beta\},
$$

则

$$
H_{\alpha\beta}
=
\sum_{P=1}^{N_{\mathrm{aux}}}
A^\alpha_P A^\beta_P.
$$

这说明：如果 same-spin pair cache 能保存 \(d\) 和 \(q_P\)，opposite-spin coupling 可由长度 \(N_{\mathrm{aux}}\) 的 dot product 得到。

## 6. \(q_P\) 的 Low-Rank Update

若 transition density 更新为

$$
T' = T+\Delta T,
$$

则

$$
q'_P
=
\operatorname{Tr}(B^P T')
=
q_P+\Delta q_P,
$$

其中

$$
\Delta q_P
=
\operatorname{Tr}(B^P\Delta T).
$$

如果

$$
\Delta T
=
\sum_{\ell=1}^r
x_\ell y_\ell^\mathsf{T},
$$

则

$$
\Delta q_P
=
\sum_{\ell=1}^r
y_\ell^\mathsf{T}B^P x_\ell.
$$

### 6.1 由 \(X\) 更新导致的 \(\Delta T\)

以 row update 为例：

$$
X'
=
X
-
\frac{X e_i u^\mathsf{T}X}{\eta}.
$$

定义

$$
a = X e_i,
\qquad
b^\mathsf{T} = u^\mathsf{T}X.
$$

则

$$
\Delta X
=
X'-X
=
-\frac{ab^\mathsf{T}}{\eta}.
$$

如果 occupied orbital support 不变，仅 \(X\) 改变，那么

$$
\Delta T
=
-\frac{1}{\eta}
\sum_{c=1}^m\sum_{d=1}^m
a_c b_d
|r_c\rangle\langle l_d|.
$$

实际 row replacement 还会把 right support 中的 \(r_i\) 换成 \(r_{\mathrm{new}}\)。因此 \(\Delta T\) 包含两类变化：

1. inverse overlap 的 rank-1 更新；
2. support row 从旧轨道 \(r_i\) 移动到新轨道 \(r_{\mathrm{new}}\)。

最稳健的第一版可以不显式维护 support-change 的闭式低秩形式，而是在更新 \(X\) 后重算 \(q_P\)。这样仍然能获得 overlap 部分的降阶。

第二版再显式维护 \(\Delta T\)，利用

$$
\Delta q_P
=
\operatorname{Tr}(B^P\Delta T)
$$

做 projection 增量。

### 6.2 \(q_P\) 更新的标度

重算：

$$
q_P
=
\sum_{a,b}
B^P_{r_a,l_b}X_{b,a}
$$

成本为

$$
O(N_{\mathrm{aux}}m^2).
$$

若显式 \(\Delta T\) 可以表示成 \(r\) 个低秩项，并且每个 \(x_\ell,y_\ell\) 的支撑大小为 \(s_x,s_y\)，则

$$
\Delta q_P
=
\sum_{\ell=1}^r
y_\ell^\mathsf{T}B^P x_\ell
$$

成本约为

$$
O(N_{\mathrm{aux}} r s_x s_y).
$$

如果单替换下有效支撑可控制在 \(O(m)\)，则目标是

$$
O(N_{\mathrm{aux}}m)
\quad \text{到} \quad
O(N_{\mathrm{aux}}m^2).
$$

因此 \(q_P\) update 的实际收益取决于能否把 support-change 也组织成小支撑增量。

## 7. Same-Spin Exchange \(k_P\)

same-spin RI two-electron contribution 可写成

$$
\phi_2^{\mathrm{same}}
=
\frac{1}{2}
\sum_{P=1}^{N_{\mathrm{aux}}}
\left[
q_P^2-k_P
\right],
$$

其中

$$
k_P
=
\operatorname{Tr}(B^P T B^P T).
$$

若重新计算 \(k_P\)，朴素矩阵形式成本为

$$
O(N_{\mathrm{aux}}m^3).
$$

这通常是 RI same-spin kernel 的难点。

## 8. \(k_P\) 的 Low-Rank Update

若

$$
T'=T+\Delta T,
$$

则

$$
\begin{aligned}
k'_P
&=
\operatorname{Tr}\left(B^P(T+\Delta T)B^P(T+\Delta T)\right)\\
&=
k_P
+
2\operatorname{Tr}\left(B^P T B^P\Delta T\right)
+
\operatorname{Tr}\left(B^P\Delta T B^P\Delta T\right).
\end{aligned}
$$

如果

$$
\Delta T=xy^\mathsf{T},
$$

则

$$
\Delta q_P
=
y^\mathsf{T}B^P x.
$$

二阶增量项为

$$
\operatorname{Tr}\left(B^P\Delta T B^P\Delta T\right)
=
\left(y^\mathsf{T}B^Px\right)^2
=
(\Delta q_P)^2.
$$

交叉项为

$$
\operatorname{Tr}\left(B^P T B^Pxy^\mathsf{T}\right)
=
y^\mathsf{T}B^P T B^P x.
$$

因此 rank-1 情况下

$$
k'_P
=
k_P
+
2y^\mathsf{T}B^P T B^P x
+
(\Delta q_P)^2.
$$

若定义

$$
w_P=B^Px,
\qquad
z_P=(B^P)^\mathsf{T}y,
$$

且 \(B^P\) 对称，则

$$
y^\mathsf{T}B^P T B^P x
=
z_P^\mathsf{T}T w_P.
$$

由于 \(T\) 只有 \(m^2\) 个非零元，

$$
z_P^\mathsf{T}T w_P
$$

的直接成本是

$$
O(m^2)
$$

per auxiliary function。因此 rank-1 update 的 \(k_P\) 成本可控制在

$$
O(N_{\mathrm{aux}}m^2),
$$

相比重算

$$
O(N_{\mathrm{aux}}m^3)
$$

降低一个 \(m\) 的标度。

如果

$$
\Delta T
=
\sum_{\ell=1}^r x_\ell y_\ell^\mathsf{T},
$$

则

$$
k'_P
=
k_P
+
2\sum_{\ell=1}^r
y_\ell^\mathsf{T}B^P T B^P x_\ell
+
\sum_{\ell=1}^r\sum_{s=1}^r
\left(y_\ell^\mathsf{T}B^Px_s\right)
\left(y_s^\mathsf{T}B^Px_\ell\right).
$$

该式是实现多 rank update 的核心公式。

## 9. 总标度对比

### 9.1 当前逐 pair 直接计算

regular pair 每次重新分解 overlap：

$$
O(m^3).
$$

same-spin RI exchange 朴素重算：

$$
O(N_{\mathrm{aux}}m^3).
$$

总成本近似：

$$
O\left(
U^2
\left[
m^3
+
N_{\mathrm{aux}}m^3
\right]
\right).
$$

### 9.2 第一阶段：只做 Sherman-Morrison

overlap 变为：

$$
O(m^2).
$$

如果 RI 部分仍重算，则：

$$
O\left(
U^2
\left[
m^2
+
N_{\mathrm{aux}}m^3
\right]
\right).
$$

收益来自 overlap/cofactor hot path。

### 9.3 第二阶段：Sherman-Morrison + 重算 \(q_P\)

如果 \(q_P\) 重算，opposite-spin projection 成本为：

$$
O(N_{\mathrm{aux}}m^2).
$$

same-spin exchange \(k_P\) 仍重算：

$$
O(N_{\mathrm{aux}}m^3).
$$

总成本：

$$
O\left(
U^2
\left[
m^2
+
N_{\mathrm{aux}}m^2
+
N_{\mathrm{aux}}m^3
\right]
\right).
$$

### 9.4 第三阶段：\(k_P\) 也 low-rank update

若 \(\Delta T\) 能维持小 rank / 小支撑，目标成本为：

$$
O\left(
U^2
\left[
m^2
+
N_{\mathrm{aux}}m^2
\right]
\right).
$$

这就是真正重要的降阶：

$$
O(U^2N_{\mathrm{aux}}m^3)
\rightarrow
O(U^2N_{\mathrm{aux}}m^2).
$$

## 10. 实现路线

### 10.1 第一优先级：Overlap Low-Rank Update

在 `build_same_spin_pair_cache()` 的 unique-spin pair 双循环中，引入按单替换顺序排列的 spin strings。

对 regular pair 保存：

$$
d,\quad X.
$$

相邻 pair 用 Sherman-Morrison 更新。

fallback 条件：

$$
|\eta|<\tau_{\mathrm{SM}}.
$$

这个阶段不改变 RI/two-electron 公式，风险最低。

### 10.2 第二优先级：缓存 \(q_P\)

对每个 same-spin pair 缓存：

$$
d,
\qquad
q_P,
\qquad
A_P=dq_P.
$$

opposite-spin coupling 使用：

$$
\sum_P A^\alpha_P A^\beta_P.
$$

第一版可重算 \(q_P\)，保证正确性。后续再做 \(\Delta q_P\)。

### 10.3 第三优先级：更新 \(k_P\)

缓存：

$$
k_P
$$

或缓存足够计算

$$
y^\mathsf{T}B^PTB^Px
$$

的中间量。

先实现 rank-1 公式：

$$
k'_P
=
k_P
+
2y^\mathsf{T}B^PTB^Px
+
(\Delta q_P)^2.
$$

再推广到 multi-rank update。

## 11. 关键风险

1. Sherman-Morrison 只适用于 regular pair；singular/nullity pair 必须 fallback。
2. row/right、column/left 的约定必须保持一致，否则 \(X\)、\(T\)、cofactor 的转置会错。
3. spin string traversal 必须保证相邻 string 单替换，否则 low-rank update 退化。
4. \(q_P\) 的完整 low-rank update 不仅要处理 \(X\) 的 rank-1 更新，还要处理 occupied support 变化。
5. \(k_P\) update 是最复杂部分，应放在 overlap update 和 \(q_P\) cache 验证后。

## 12. 可验证的 RI Channel 形式

对固定 same-spin pair，定义 occupied RI block：

$$
B^P_{RL}[a,b]=B^P_{r_a,l_b}.
$$

令

$$
M_P=B^P_{RL}X.
$$

则

$$
q_P=\operatorname{Tr}(M_P),
$$

并且

$$
k_P=\operatorname{Tr}(M_P^2).
$$

same-spin RI contribution 为

$$
\phi_2^{\mathrm{same}}
=
\frac{1}{2}
\sum_P
\left[
\operatorname{Tr}(M_P)^2
-
\operatorname{Tr}(M_P^2)
\right].
$$

### 12.1 Row Replacement 下的 \(M_P\) 更新

沿用

$$
S'=S+e_i u^\mathsf{T},
\qquad
X'=X-\frac{X e_i u^\mathsf{T}X}{\eta}.
$$

定义

$$
\rho^\mathsf{T}=u^\mathsf{T}X,
\qquad
\eta=1+\rho_i.
$$

right string 第 \(i\) 个占据轨道从 \(r_i\) 替换为 \(r_{\mathrm{new}}\)。RI block 第 \(i\) 行变化为

$$
\delta b_P^\mathsf{T}[b]
=
B^P_{r_{\mathrm{new}},l_b}
-
B^P_{r_i,l_b}.
$$

定义

$$
\delta m_P^\mathsf{T}
=
\delta b_P^\mathsf{T}X.
$$

旧 channel 为 \(M_P\)，其第 \(i\) 列为

$$
c_P=M_P e_i.
$$

row replacement 后

$$
M'_P
=
M_P
+
e_i\delta m_P^\mathsf{T}
-
\frac{
\left(c_P+\delta m_{P,i}e_i\right)\rho^\mathsf{T}
}{\eta}.
$$

这是两个 rank-1 项：

$$
\Delta M_P
=
a_1 b_1^\mathsf{T}
+
a_2 b_2^\mathsf{T},
$$

其中

$$
a_1=e_i,
\qquad
b_1=\delta m_P,
$$

$$
a_2=-\frac{1}{\eta}
\left(c_P+\delta m_{P,i}e_i\right),
\qquad
b_2=\rho.
$$

### 12.2 标量 \(q_P\) 和 \(k_P\) 的增量

trace 更新为

$$
q'_P
=
q_P+\operatorname{Tr}(\Delta M_P),
$$

即

$$
q'_P
=
q_P
+
b_1^\mathsf{T}a_1
+
b_2^\mathsf{T}a_2.
$$

对

$$
k_P=\operatorname{Tr}(M_P^2),
$$

有

$$
k'_P
=
k_P
+
2\operatorname{Tr}(M_P\Delta M_P)
+
\operatorname{Tr}(\Delta M_P^2).
$$

因为 \(\Delta M_P\) 是两个 rank-1 项，

$$
\operatorname{Tr}(M_P\Delta M_P)
=
b_1^\mathsf{T}M_Pa_1
+
b_2^\mathsf{T}M_Pa_2,
$$

且

$$
\operatorname{Tr}(\Delta M_P^2)
=
\sum_{\alpha=1}^2
\sum_{\beta=1}^2
\left(b_\alpha^\mathsf{T}a_\beta\right)
\left(b_\beta^\mathsf{T}a_\alpha\right).
$$

因此每个 auxiliary channel 的更新成本为

$$
O(m^2),
$$

而不是重新计算 \(M_P=B^P_{RL}X\) 和 \(\operatorname{Tr}(M_P^2)\) 的

$$
O(m^3).
$$

总目标标度为

$$
O(U^2N_{\mathrm{aux}}m^3)
\rightarrow
O(U^2N_{\mathrm{aux}}m^2).
$$

## 13. Synthetic Benchmark

验证工具：

```bash
OMP_NUM_THREADS=1 build/src/benchmark_low_rank_synthetic --min-m 4 --max-m 10 --naux 1024 --steps 1000
```

这里使用闭壳层 synthetic case：

$$
N=2m,
\qquad
M=2m,
\qquad
n_\alpha=n_\beta=m.
$$

每一步对 right spin string 做一次 single row replacement。直接算法重新分解 overlap，并重新计算 RI same-spin \(\phi_2\)。low-rank 算法用 Sherman-Morrison 更新 \(X,d\)，并用上面的 rank-1 channel 公式维护 \(q_P,k_P\)。

结果如下：

| \(m\) | \(N\) | \(N_{\mathrm{aux}}\) | steps | overlap speedup | RI \(\phi_2\) speedup | max \(|\Delta\phi_2|\) |
|---:|---:|---:|---:|---:|---:|---:|
| 4 | 8 | 1024 | 1000 | 3.14 | 0.64 | \(6.4\times 10^{-9}\) |
| 5 | 10 | 1024 | 1000 | 4.33 | 0.92 | \(1.8\times 10^{-8}\) |
| 6 | 12 | 1024 | 1000 | 4.50 | 1.21 | \(5.9\times 10^{-8}\) |
| 7 | 14 | 1024 | 1000 | 5.42 | 1.96 | \(3.3\times 10^{-7}\) |
| 8 | 16 | 1024 | 1000 | 5.76 | 2.08 | \(4.1\times 10^{-6}\) |
| 9 | 18 | 1024 | 1000 | 6.00 | 2.51 | \(3.9\times 10^{-6}\) |
| 10 | 20 | 1024 | 1000 | 6.04 | 3.05 | \(7.4\times 10^{-6}\) |

对应 \(m=10\) 即 \(20\) active orbitals、\(20\) active electrons 的闭壳层 synthetic case。

## 14. 当前结论

当前最值得推进的主线是：

$$
\boxed{
\text{single-replacement spin-string traversal}
+
\text{Sherman-Morrison}
+
\text{RI projection cache}
+
\text{\(k_P\) low-rank update}
}
$$

第一阶段可以稳定实现：

$$
O(m^3)\rightarrow O(m^2)
$$

for overlap inverse/determinant。

最终目标是：

$$
O(U^2N_{\mathrm{aux}}m^3)
\rightarrow
O(U^2N_{\mathrm{aux}}m^2).
$$

如果这个目标能在 regular unique-spin pairs 上成立，它会直接改善当前瓶颈，因为它降低的是每个 unique-spin pair kernel 的成本。

目前 synthetic benchmark 支持这个判断：小 \(m\) 时 RI channel update 常数项不占优，但从 \(m\ge 6\) 开始出现收益，到 \(m=10\) 约为 \(3.0\times\)。这说明 low-rank-update 思路还没有被否定，下一步应在真实 unique-spin-string traversal 上实现相同的 channel-stat 更新，而不是只验证 isolated synthetic kernel。
