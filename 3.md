这个 $T_a \Delta X_a$ 项的出现，是整个非正交流形优化中最漂亮的一笔。它实质上是通过**消除内部正交旋转的规范冗余（Gauge Redundancy）**，将抽象的 SPD（对称正定）流形形变直接“降维打击”映射到了物理基底 $T_a$ 上。

下面我为你一步步严密推导，这个项到底是怎么从满秩的参数空间里被剥离出来的，以及为什么它是一个对称矩阵。

---

### 第一步：从完备基展开出发

在任何局部轨道优化中，活性轨道 $C_A$ 的一阶变分 $\delta C_A$ 必须能够在整个分子空间的完备基 $\{Q_i, T_a, T_v\}$ 上展开。

根据物理轨道的定义：
$$C_A = Q_i G_a + T_a$$
对两边求变分：
$$\delta C_A = (\delta Q_i) G_a + Q_i (\delta G_a) + \delta T_a$$

其中，前面的部分已经在我们上一条的推导中通过 $K_{iv}$ 和 $K_{ia}$ 搞定了（即非活性空间的倾斜和规范拖拽）。现在我们的全部焦点只集中在**活性空间内部的自我演化**，也就是 $\delta T_a$ 在其自身所在子空间的投影：
$$\delta_{aa} T_a$$

既然 $T_a$ 本身就张成了 $n_a$ 维的活性子空间，那么它自身的任何内部变化，一定可以写成 $T_a$ 右乘某个 $n_a \times n_a$ 的一般矩阵 $\Delta M$：
$$\delta_{aa} T_a = T_a \Delta M$$

现在的问题转化为：**这个 $n_a \times n_a$ 的矩阵 $\Delta M$ 包含了 $n_a^2$ 个参数，它有冗余吗？如何把它压缩成无冗余的 $\Delta X_a$？**

---

### 第二步：引入度规 $M_{aa}$ 与形状变化

在 VBSCF 中，活性空间真正的物理自由度（变分自由度）完全由其**非正交重叠矩阵（度规）**的改变来刻画：
$$M_{aa} = T_a^{\mathrm T} S T_a$$

我们来看看，当我们施加扰动 $\delta_{aa} T_a = T_a \Delta M$ 时，这个度规发生了什么变化？根据乘积法则：
$$
\begin{aligned}
\delta M_{aa} &= (\delta_{aa} T_a)^{\mathrm T} S T_a + T_a^{\mathrm T} S (\delta_{aa} T_a) \\
&= (T_a \Delta M)^{\mathrm T} S T_a + T_a^{\mathrm T} S (T_a \Delta M) \\
&= \Delta M^{\mathrm T} (T_a^{\mathrm T} S T_a) + (T_a^{\mathrm T} S T_a) \Delta M \\
&= \Delta M^{\mathrm T} M_{aa} + M_{aa} \Delta M
\end{aligned}
$$

这是一个极其关键的方程！它告诉我们：矩阵 $\Delta M$ 是如何改变物理重叠矩阵的。

---

### 第三步：剔除“零模”（Zero Mode），逼出对称性

任何一个一般方阵 $\Delta M$ 都可以进行唯一的**李代数分解（相对于度规 $M_{aa}$）**，拆分成一个“形变部分”和一个“旋转部分”：
$$\Delta M = \Delta X_a + \Omega_a$$

我们对这两个部分施加严格的数学定义：
1. **纯形变部分（Pure Strain）** $\Delta X_a$：取为标准的对称矩阵，即 $\Delta X_a^{\mathrm T} = \Delta X_a$。
2. **纯旋转部分（Pure Rotation）** $\Omega_a$：取为相对于 $M_{aa}$ 的反称生成元，即 $\Omega_a^{\mathrm T} M_{aa} + M_{aa} \Omega_a = 0$。

现在，把这个分解代入我们刚刚得到的度规变化公式中：
$$
\begin{aligned}
\delta M_{aa} &= (\Delta X_a + \Omega_a)^{\mathrm T} M_{aa} + M_{aa} (\Delta X_a + \Omega_a) \\
&= (\Delta X_a^{\mathrm T} M_{aa} + M_{aa} \Delta X_a) + \underbrace{(\Omega_a^{\mathrm T} M_{aa} + M_{aa} \Omega_a)}_{=0 \text{ (根据反称定义)}} \\
&= \Delta X_a M_{aa} + M_{aa} \Delta X_a
\end{aligned}
$$

**物理图像的顿悟：**
你看到了吗？那个代表内部旋转的 $\Omega_a$ 矩阵，在计算度规变化 $\delta M_{aa}$ 时**完美对消，彻底消失了！** 

这意味着，$\Omega_a$ 是一个彻头彻尾的**规范冗余（Gauge Redundancy）**。它只会让轨道在活性空间内部“原地打转”，而不会改变任何非正交轨道的重叠性质（即不会改变物理流形的 Shape）。如果把它留在优化参数里，雅可比矩阵 $J$ 在这个方向上的梯度绝对是 0，导致 Hessian 奇异。

---

### 第四步：最终的切向截断

为了进行非冗余的流形优化（Manifold Optimization），我们必须在切空间中**强制施加规范固定条件（Gauge Fixing Condition）**，即：
**“禁止任何不改变度规的纯旋转！”**

在数学上，这等价于直接令：
$$\Omega_a = 0$$

于是，原本一般形式的参数 $\Delta M$ 就被严格“压缩”成了一个纯对称矩阵 $\Delta X_a$：
$$\Delta M \equiv \Delta X_a \in \mathrm{Sym}(n_a)$$

将其代回第一步的公式，我们就得到了活性空间内部形变的终极无冗余表达式：
$$\delta_{aa} T_a = T_a \Delta X_a$$

---

### 总结：从数学框架到你的公式

回过头来看你写的最终切线公式：
$$
\delta C_A = T_v (K_{av} + K_{iv} G_a) + C_I K_{ia}^{\mathrm T} + T_a \Delta X_a
$$

其中 $T_a \Delta X_a$ 的推导逻辑链条是：
1. **$T_a (\dots)$**：因为它是活性空间内部的变化，所以以 $T_a$ 为基底展开。
2. **剔除旋转 $\Omega_a$**：为了消除不改变物理重叠的零模冗余，抛弃了所有的反称/旋转自由度。
3. **保留对称 $\Delta X_a$**：剩下的对称矩阵刚好拥有 $\frac{n_a(n_a+1)}{2}$ 个参数，这与 SPD 流形（正定重叠矩阵空间）的切空间维度分毫不差。

这个推导过程彻底绕开了 $Q_a$ 的显式极分解（不用解变态的 Sylvester 方程），直接利用 $M_{aa}$ 的李代数直和分解得到了最优的参数化策略。极其优雅！